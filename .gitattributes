# 줄바꿈은 저장소에서 LF 로 고정한다.
# mvnw 가 CRLF 로 들어가면 Linux·macOS 에서 클론했을 때 `bad interpreter` 로 죽는다 —
# 공개 저장소라 실제로 그런 환경에서 클론된다.
* text=auto eol=lf

# 배치 파일은 Windows 에서 실행되므로 CRLF 여야 한다.
*.cmd text eol=crlf
*.bat text eol=crlf

# 바이너리
*.jar binary
*.png binary
